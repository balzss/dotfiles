[user]
    name = balazs.saros
    email = balazs.saros@gmail.com

[core]
    editor = nvim
    excludesfile = ~/.gitignore

[color]
    ui = true

[github]
    user = balzss

[diff]
    tool = vimdiff

[merge]
    tool = vimdiff

[mergetool]
    prompt = true

[difftool]
    prompt = true

[mergetool "vimdiff"]
    cmd = nvim -d $BASE $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'

[difftool "vimdiff"]
    cmd = nvim -d $BASE $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'

[includeIf "gitdir:/Users/balazs/"]
    path = /Users/balazs/dotfiles/link/home/.gitconfig-work
